apply plugin: 'application'

mainClassName = 'store.DatastoreMain'
jar {
    manifest {
        attributes 'Main-Class': 'store.DatastoreMain'
    }
}

dependencies {
    compile project(':api')

    testCompile project(':client')
}
