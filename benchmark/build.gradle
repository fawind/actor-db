repositories {
    flatDir {
        dirs 'dependencies'
    }
}

dependencies {
    compile files('dependencies/ycsb-core-0.14.0.jar')
    compile project(path: ':client', configuration: 'shadow')
    compile project(':api')
}
